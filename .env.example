# Copy this file to .env.local and fill in your key.
#
# Used to draft audits with Claude. Without it, the app falls back to
# template-based drafts (no model call). Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=
